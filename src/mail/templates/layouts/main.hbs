<html>
    <head>
        <meta charset='utf-8' />
        <style>
            body {
                font-family: Arial, sans-serif;
                margin: 0;
                padding: 0;
                background-color: #f4f4f4;
            }
            .wrapper {
                max-width: 600px;
                margin: 20px auto;
                background: #ffffff;
                border-radius: 8px;
                overflow: hidden;
            }
            .content-area {
                padding: 30px;
                color: #333;
                line-height: 1.5;
            }
        </style>
    </head>
    <body>
        <div class='wrapper'>

            <div class='content-area'>
                {{{body}}}
                <!-- Inyecta el contenido (ej. reset-password) -->
            </div>

        </div>
    </body>
</html>